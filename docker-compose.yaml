version: "3.8"

services:
  backend:
    build: ./backend
    ports: 
      - 80:8000
    # command: ./docker-entrypoint.sh
    # volumes:
    #   - taxi:/data
  
  frontend:
    depends_on:
      - backend
    build: ./frontend
    ports:
      - 80:3000

# volumes:
#   taxi:
